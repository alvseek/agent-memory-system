# Quality Standard

<!-- ============================================================
  QUALITY STANDARD TEMPLATE — Project-Specific Quality Criteria

  HOW TO USE:
  1. Copy this file into your project via `/generate-standard`
  2. The agent investigates the codebase and fills each dimension
  3. Delete the HTML comments as you fill each section
  4. Keep all 9 dimensions — mark irrelevant ones "Not applicable"
  5. Use unknown markers for uncertain discoveries:
     - [CONFIRM: ...] — found a pattern, unsure if intentional convention
     - [TODO: ...] — needs human input, not discoverable from code
     - [NOT FOUND: ...] — expected a convention but couldn't locate evidence

  RELATIONSHIP TO CODE QUALITY ANALYSIS:
  This file provides the PROJECT-SPECIFIC criteria checked by
  Dimension 8 of the Code Quality Analysis Template. Dimensions
  1-7 here ADD to (not replace) the generic checks in the
  analysis template. Dimension 8 here covers tooling. Dimension 9
  catches everything else.
============================================================ -->

## Project Info

- **Project**: [Project Name]
- **Date Generated**: [YYYY-MM-DD]
- **Generated By**: [Agent Name]
- **Tech Stack**: [e.g., Node.js + NestJS + PostgreSQL, Python + FastAPI, .NET 8 + EF Core]

---

## 1. Error Handling

<!-- tip: Document how THIS project handles errors — not generic best practices.
     Look for: centralized error classes, error response formats, logging patterns,
     retry mechanisms, error boundaries. -->

- [ ] [e.g., All API errors use `AppException` class with error code + message]
- [ ] [e.g., Error responses follow `{ success: false, error: { code, message } }` format]
- [ ] [e.g., All external service calls wrapped in try/catch with specific error types]

---

## 2. State Completeness

<!-- tip: Document how THIS project handles UI states.
     Look for: loading component patterns, empty state components, error display
     patterns, skeleton loaders, toast/notification system. -->

- [ ] [e.g., All async pages use `<LoadingSpinner />` during data fetch]
- [ ] [e.g., Empty collections show `<EmptyState />` with action prompt]
- [ ] [e.g., Form submissions disable the button and show inline spinner]

*Not applicable — backend only* (use this line if the project has no UI)

---

## 3. UX Completeness

<!-- tip: Document how THIS project handles user interaction flows.
     Look for: modal/drawer patterns, confirmation dialogs, toast notifications,
     form validation approach, navigation patterns. -->

- [ ] [e.g., All modals use `<BaseModal />` with close button + Escape key + backdrop click]
- [ ] [e.g., Destructive actions require `<ConfirmDialog />` with typed confirmation]
- [ ] [e.g., All user actions show success/error toast via `useNotification()` hook]

*Not applicable — backend only* (use this line if the project has no UI)

---

## 4. Efficiency

<!-- tip: Document performance conventions specific to THIS project.
     Look for: caching strategies, query optimization patterns, pagination
     conventions, memoization patterns, lazy loading rules. -->

- [ ] [e.g., All list endpoints use cursor-based pagination with `limit` + `cursor` params]
- [ ] [e.g., Database queries use repository pattern — no raw queries in controllers]
- [ ] [e.g., React components with expensive renders use `React.memo` or `useMemo`]

---

## 5. Security

<!-- tip: Document security patterns specific to THIS project.
     Look for: auth middleware, role/permission patterns, input validation
     approach, CORS config, rate limiting, sensitive data handling. -->

- [ ] [e.g., All protected routes use `@UseGuards(AuthGuard)` decorator]
- [ ] [e.g., Input validation uses class-validator DTOs — no manual validation]
- [ ] [e.g., Sensitive fields (password, token) excluded via `@Exclude()` in response DTOs]

---

## 6. Cleanup

<!-- tip: Document cleanup conventions specific to THIS project.
     Look for: linter rules about console.log, dead code policies, TODO
     conventions, import ordering rules. -->

- [ ] [e.g., ESLint rule `no-console` enforced — use `Logger` service instead]
- [ ] [e.g., Unused imports auto-removed by Prettier organize-imports plugin]
- [ ] [e.g., TODOs must include issue ticket number: `// TODO(PROJ-123): description`]

---

## 7. Consistency

<!-- tip: Document naming and structural conventions specific to THIS project.
     Look for: file naming patterns, folder structure rules, naming conventions
     (camelCase vs snake_case), component organization, module patterns. -->

- [ ] [e.g., Files use kebab-case: `user-profile.service.ts`, `create-order.dto.ts`]
- [ ] [e.g., Folder structure follows feature-based modules: `src/modules/{feature}/`]
- [ ] [e.g., All services are injectable singletons with constructor injection]
- [ ] [e.g., API endpoints follow REST conventions: plural nouns, nested resources]

---

## 8. Tooling & Linter Configuration

<!-- tip: Document what tools THIS project uses for code quality enforcement.
     Look for: ESLint/Prettier/Biome config, pre-commit hooks, CI checks,
     TypeScript strictness level, test coverage requirements. -->

- [ ] [e.g., ESLint with `@typescript-eslint/recommended` — strict mode enabled]
- [ ] [e.g., Prettier with 100 char line width, single quotes, trailing commas]
- [ ] [e.g., TypeScript `strict: true` with `noUncheckedIndexedAccess`]
- [ ] [e.g., Pre-commit hook runs `lint-staged` (lint + format check)]
- [ ] [e.g., CI pipeline requires 80%+ test coverage for merge]

---

## 9. Project-Specific Conventions

<!-- tip: Catch-all for conventions that don't fit dimensions 1-8.
     Look for: Git workflow, commit message format, PR conventions, API
     versioning strategy, deployment rules, environment management,
     documentation standards, dependency management rules. -->

- [ ] [e.g., Git commits follow Conventional Commits: `feat:`, `fix:`, `chore:`]
- [ ] [e.g., PRs require at least 1 approval before merge]
- [ ] [e.g., API versioning via URL path: `/api/v1/`, `/api/v2/`]
- [ ] [e.g., Environment variables documented in `.env.example` — never commit `.env`]
- [ ] [e.g., New dependencies require team discussion before adding]

---
